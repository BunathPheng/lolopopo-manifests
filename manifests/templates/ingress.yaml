---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: lolopopo                  # Ingress name
  labels:
    app: lolopopo
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "lolopopo.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: lolopopo               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
